console.log(divrange(25200, [3, 7, 200, 111, 33]));

function divrange(inputn, divby){
  let count = 0;
  let divbyarray = [];
  while(count !== 1000 && divby.length !== 0){
    if (inputn % divby[0] === 0) {
      divbyarray.push(divby[0]);
      divby.shift();
      
    } else {
      divby.shift();
    }
        
    count++;
  };
  while (hello) {
    
    
  }
  divbyarray.sort();
  divbyarray.reverse();
  return divbyarray
};
