console.log(min(0, 10));
// → 0
console.log(min(-5, -5));
// → -10

function min(a, b) {if (a > b) {
    return b
  } else if (b > a) {
    return a
  } else if (a === b) {
    console.log(`${a} and ${b} are the same`);
    return a
  } else if (b === undefined) {
    console.log(`The opposite of ${a} is:`);
    return (a * -1)
  } else {
    console.log(`${a} and ${b} cannot be compared`)
  };
  
}
